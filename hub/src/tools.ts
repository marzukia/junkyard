import type { AppTag as _AppTag, Category } from "../../scripts/catalogue-schema.ts";

// Yard is the hub-internal name for catalogue Category.
export type Yard = Category;
export type AppTag = _AppTag;

export interface Tool {
  name: string;
  slug: string;
  yard: Yard;
  tagline: string;
  /** The paid incumbent this tool replaces. Empty string = show "free forever". */
  incumbent: string;
  tags?: AppTag[];
  mcpExposed: boolean;
}

export const YARDS: { id: Yard; label: string }[] = [
  { id: "image", label: "Image & Media" },
  { id: "text", label: "Text & Code" },
  { id: "ai", label: "In-browser AI" },
  { id: "docs", label: "Docs & Utility" },
];

// Order is canonical; card numbers (#NN) are derived from index + 1.
// Source of truth lives in apps/*/junkyard.ts; generated by scripts/gen-catalogue.ts.
export { TOOLS } from "./catalogue.generated";
